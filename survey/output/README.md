# Survey Output

This folder is for generated local run output.

The harness foundation writes reports and validator results under this tree.

Common child folders:

- `english-log/`
- `harness-validator/`
- `runs/`

Generated output should stay local unless it is intentionally reviewed for commit.

Use `survey/fixtures/` for tracked synthetic input and examples.
