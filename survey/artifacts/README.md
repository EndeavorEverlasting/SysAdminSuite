# Survey Artifacts

This folder is for local generated artifacts created by survey and harness runs.

Tracked files here should normally be limited to this README or carefully reviewed synthetic examples.

Use related paths as follows:

- `survey/input/` for local staging
- `survey/output/` for generated run output
- `survey/fixtures/` for tracked synthetic fixtures
