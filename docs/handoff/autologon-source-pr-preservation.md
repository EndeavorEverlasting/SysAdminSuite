# AutoLogon Source PR Preservation Ledger

## Summary
This ledger records the disposition of all stale AutoLogon and package-intake source PRs (#167, #168, #172, #185, #188, #189, #191, #192, #195) evaluated against current `main` (floor after PR #253 / #254 / #255).

## Preservation Ledger

| Source PR | PR Title | Source Commits / Key Files | Preservation Destination | Disposition & Action Taken | Rejected Portions & Rationale | Downstream Owner |
|---|---|---|---|---|---|---|
| **#167** | AutoLogon state delta | `Invoke-SasAutoLogonStateDelta.ps1`, `Start-SasAutoLogonStateDelta.ps1`, `Run-AutoLogonStateDelta.cmd`, `AUTOLOGON_STATE_DELTA.md` | Preserved on `main` via PR #193 (`04cea35`); aligned with PR #253 | **Preserved & Ready to Close** | Stale PR check workflow (`patch-autologon-state-delta.yml`) rejected as temporary diagnostic. Unresolved password-presence P1 classifier repair was merged into `main`. | P11 (state proof emitter) |
| **#168** | AutoLogon deployment/runtime workflow | `Invoke-SasAutoLogonDeployment.ps1`, `Invoke-SasAutoLogonFileAccessPosture.ps1`, `Invoke-SasAutoLogonSessionAccessProof.ps1`, `Invoke-SasAutoLogonTechnicianRuntimeProof.ps1`, `Start-SasAutoLogonTechnicianRuntimeProof.cmd`, `AUTOLOGON_DEPLOYMENT_WORKFLOW.md`, `AUTOLOGON_FILE_ACCESS_POSTURE.md`, `AUTOLOGON_SESSION_ACCESS_PROOF.md`, `AUTOLOGON_TECHNICIAN_RUNTIME_PROOF.md` | Emitters & docs preserved on `main` via PR #193 (`04cea35`); deployment refactored via PR #255 & PR #253 floor | **Preserved & Ready to Close** | Legacy direct WinRM deployment path in `Invoke-SasAutoLogonDeployment.ps1` rejected as superseded by canonical Kerberos SMB scheduled-task transport. | P10 (admin deploy) / P11 (proof emitters) |
| **#172** | AutoLogon and server install operator tutorials | `START-HERE-SysAdminSuite.md` | Preserved on `chore/autologon-source-pr-preservation` | **Preserved & Ready to Close** | None. Break-glass local check and server install tutorial additions integrated onto current-main guide. | P14 (technician pack) |
| **#185** | Recover approved catalog manifest and acceptance workflows | `Invoke-SasApprovedSoftwareAcceptance.ps1`, `Invoke-SasAuthorizedDeploymentManifest.ps1`, `New-SasAuthorizedDeploymentManifest.ps1`, `APPROVED_SOFTWARE_ACCEPTANCE.md`, `authorized-deployment-manifest.schema.json` | Preserved on `main` via PR #193 (`04cea35`) | **Preserved & Ready to Close** | Stale configuration manifests rejected in favor of frozen #253 harness API and approved app schema. | P10 (package reference) / P14 (technician pack) |
| **#188** | Fail-closed host eligibility gate | `Test-SasHostEligibility.ps1`, `host-eligibility-policy.schema.json`, `host-eligibility-policy.sample.json`, `HostEligibility.Tests.ps1` | Preserved on `main` via PR #193 (`04cea35`) | **Preserved & Ready to Close** | None. Core host eligibility script, schema, and sample policy are merged on main. | P10 (final gate preflight) |
| **#189** | Read-only local package inventory contract | `Get-SasLocalPackageInventory.ps1`, `local-package-inventory.schema.json`, `local-package-inventory.fixture.json` | Preserved on `main` via PR #193 (`04cea35`); upgraded via PR #195 | **Preserved & Ready to Close** | Unredacted local path defaults and unproven default installer arguments rejected in favor of PR #195 evidence floor. | P10 / P16 (package analysis) |
| **#191** | AutoLogon final-step gate | `Invoke-SasAutoLogonFinalStepGate.ps1`, `AUTOLOGON_FINAL_STEP_CONTRACT.md`, `AUTOLOGON_PHYSICAL_PILOT_CHECKLIST.md`, `AUTOLOGON_UNIQUE_BEHAVIOR_MATRIX.md`, `AutoLogonFinalStepGate.Tests.ps1` | Preserved on `main` via PR #193 (`04cea35`); aligned with PR #253 floor | **Preserved & Ready to Close** | Direct legacy WinRM invocation in final-step gate rejected; refactored to consume Kerberos SMB transport preflight. | P10 (final-step gate) |
| **#192** | Fail-closed Cybernet host eligibility gate | `Config/host-eligibility-policy.sample.json`, `Test-SasHostEligibility.ps1` (Cybernet extension) | Retained as DRAFT | **Blocked / Kept Open (Draft)** | Incomplete draft PR; extended Cybernet policy patterns belong to separate Cybernet hardware/network scope. | Cybernet hardware lane (outside AutoLogon floor) |
| **#195** | Repair package inventory evidence floor | `docs/LOCAL_PACKAGE_INVENTORY_EVIDENCE_FLOOR.md`, `Tests/survey/test_local_package_inventory_evidence_floor.py`, `Get-SasLocalPackageInventory.ps1`, `local-package-inventory.schema.json`, `local-package-inventory.fixture.json` | Preserved on `chore/autologon-source-pr-preservation` | **Preserved & Ready to Close** | None. Clean fail-closed redaction, relative paths, and evidence floor contracts preserved. | P16 (package-analysis stack) |

## Key Findings & Verification
1. **Password-Presence Gate (Task 3):** Verified that `scripts/Invoke-SasAutoLogonStateDelta.ps1` on `main` requires `DefaultPassword` presence to return `autologon_ready`, returning `configured_password_missing` otherwise. No readiness overclaim is preserved.
2. **Clean Evidence Boundary (Task 4):** Checked all preserved files for machine-local UNC paths, real hostnames, passwords, credentials, and unproven arguments. All fixtures use sanitized placeholders (`operator-local-reference`, `WORKSTATION001`).
3. **No Unsafe Closures:** Only PRs whose useful work is fully present on `main` or `chore/autologon-source-pr-preservation` are marked for closure. Remote branches are kept intact.
