# Runtime log output — never commit live run logs
Mapping/Logs/*
Mapping/logs/*
!Mapping/Logs/.gitkeep

# GUI worker run sessions (contain PII: usernames, paths, PIDs)
Mapping/Output/GuiRuns/
mapping/Output/GuiRuns/
Mapping/Output/*.json
mapping/Output/*.json

# Python
__pycache__/
*.py[cod]
*.pyo

# PowerShell transcript leftovers
*.log

# Windows artifacts
Thumbs.db
desktop.ini

# Temporary / editor
*.tmp
*.bak
*.beforeSafeProps.bak
.vscode/settings.json

# Sensitive exports (CSV results from live runs)
Mapping/Config/runs/*.txt.live
Config/exports/

# GetInfo output artifacts (generated at runtime per probe)
GetInfo/Output/

# GUI lifecycle/crash output artifacts
GUI/Output/

# Survey runtime artifacts (may contain live hostnames, MACs, serials, sites, or discovery evidence)
survey/input/*
survey/output/*
survey/artifacts/*
!survey/input/.gitkeep
!survey/output/.gitkeep
!survey/artifacts/.gitkeep

# Deployment tracker reconcile outputs
DeploymentTracker/Output/*
!DeploymentTracker/Output/.gitkeep

# Deployment tracker data workspace
# Keep folder structure, but do not accidentally commit live trackers or generated evidence.
data/raw/*
data/backups/*
data/experiments/*
data/outputs/*
data/updated/*
!data/raw/.gitkeep
!data/backups/.gitkeep
!data/experiments/.gitkeep
!data/outputs/.gitkeep
!data/updated/.gitkeep

# Live workbook/data artifacts require explicit private handling or sanitization.
*.xlsx
*.xlsm
*.xls
*.csv
*.tsv
!Config/cybernet_location_prefixes.example.csv
!input/cybernet-serials.example.csv
!input/site-subnets.example.csv
!survey/fixtures/ad_registered_cybernet.sample.csv
!survey/fixtures/ad_evidence_manifest.sample.csv
!survey/fixtures/ad_network_evidence.sample.csv
!survey/fixtures/ad_live_serial.sample.csv
!survey/fixtures/ad_probe_states.sample.csv
!dashboard/samples/*.csv
*.zip
!**/.gitkeep

# Dashboard parser regression fixtures (synthetic demo data only)
!dashboard/samples/*.csv
!dashboard/samples/toolbox-status-*.json
!dashboard/fixtures/*.json

# Live toolbox probe output (written by launcher)
dashboard/toolbox-status.json

# Inventory outputs (generated by inventory/compare workflows)
inventory/**/installed_software_*.csv
inventory/**/installed_software_*.html
inventory/software_superset.csv
inventory/comparisons/run_*/

# Temp diagnostic / scratch scripts at repo root
_*

# Node (if any tooling gets added)
node_modules/

# Local OCR reference PDF (not tracked)
OCR/Jude's 2026 Buildout Project.pdf

# Portable build output (local only)
dist/

# Local tool binaries (naabu, normalizers — downloaded/built at runtime)
bin/*
!bin/.gitkeep

# .NET build output (managed projects under src/ and managed-tests/)
**/bin/
**/obj/
# Published host output staging (see docs/GUI_HOST_MIGRATION.md)
tools/publish/
# Downloaded Microsoft .NET bootstrap installers (system-wide install cache)
tools/cache/

# Local reference tree (install scripts, shortcuts, installers — not product code; never commit)
tech emulation/
Alex Emulation/

# Generated runtime artifacts from bash/apps scripts (timestamped logs and worker scripts)
bash/apps/output/

# Cybernet subnet survey runtime evidence (local only)
logs/network_context/*
logs/nmap/*
!logs/network_context/.gitkeep
!logs/nmap/.gitkeep
logs/targets/*
!logs/targets/.gitkeep

# Mini-probe/WMI batch runtime evidence (hostnames, serials, IPs, debug traces — live field data)
logs/mini-probe/*
!logs/mini-probe/.gitkeep

# Northwell registry-first verification evidence (raw reg.exe output — live field data)
logs/registry/*
!logs/registry/.gitkeep

# Cybernet subnet discovery runtime evidence (serials, IPs, DNS — live field data)
evidence/*
!evidence/.gitkeep

# Targets intake hub — local/live evidence stays out of git (see docs/TARGETS_FOLDER_POLICY.md)
targets/local/**
targets/live/**
targets/incoming/**
targets/raw/**
!targets/local/.gitkeep
!targets/live/.gitkeep
!targets/incoming/.gitkeep
!targets/raw/.gitkeep

# Tracked targets hub artifacts (docs, schemas, sanitized fixtures only)
!targets/README.md
!targets/**/*.md
!targets/**/*.schema.json
!targets/sanitized/**/*.sample.csv
!targets/sanitized/**/*.example.csv
!targets/sanitized/**/*.fixture.csv
!targets/sanitized/**/*.sample.json
!targets/sanitized/**/*.example.json
!targets/sanitized/**/*.fixture.json
Software Tracker.xlsx
*.real.xlsx
data/private/
